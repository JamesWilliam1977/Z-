interface:
  display_name: "Z"
  short_description: "Repo-specific patterns and workflows for Z-"
  default_prompt: "Use the Z- repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true